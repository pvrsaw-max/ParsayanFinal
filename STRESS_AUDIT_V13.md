# Parsayan V13 — Stress / Edge-Case Audit

## نتیجه
- Stress / Edge Cases: **34/34 PASS**
- Deep Audit: **34/34 PASS**
- Audit Regression: **10/10 PASS**
- Full Flow: **19/19 PASS**
- Secret / Resume / Tie: **18/18 PASS**

## باگ‌های جدیدی که این دور پیدا و اصلاح شدند
1. `commitTurn` از نظر موتور می‌توانست بدون ثبت نتیجه سؤال اجرا شود و نوبت را جلو ببرد. اکنون فقط پس از Resolve شدن سؤال مجاز است.
2. موتور اجازه می‌داد بعضی Powerهای قبل از سؤال مستقیماً وسط سؤال فعال شوند. اکنون Hunt/Double/Insurance/Duel/Coup فقط در Board و Swap فقط در Question مجاز است.
3. `duelResult` مستقل از فاز بازی بود؛ در State دستکاری‌شده می‌شد وسط سؤال یا بین دو بازیکن غیر از صاحب نوبت دوئل ثبت کرد. اکنون دوئل فقط در Board و با مشارکت صاحب نوبت معتبر است.
4. مصرف Power دوئل قبلاً در UI انجام می‌شد، نه Engine. بنابراین فراخوانی مستقیم Engine می‌توانست محدودیت یک‌بار مصرف را دور بزند. مصرف و Guard به Engine منتقل شد.
5. مرز دقیق پایان تایمر حراج (`now === deadline`) هنوز پاسخ درست را قبول می‌کرد. اکنون در خود لحظه deadline منقضی محسوب می‌شود.
6. State خراب/دستکاری‌شده در hydrate می‌توانست بازیکن تکراری، نام prototype، turn خارج محدوده، round غیرمجاز یا phase ناشناخته نگه دارد. hydrate اکنون این موارد را پاک/ترمیم می‌کند.
7. State دستکاری‌شده می‌توانست Power شخصی را روی Challenge وارد کند. Engine اکنون مستقل از UI آن را رد می‌کند.
8. UI نتیجه دوئل قبلاً خودش `usedPowers` را دستکاری می‌کرد؛ اکنون منبع حقیقت فقط Engine است.

## سناریوهای فشار تست‌شده
۲ بازیکن، ۱۰ بازیکن، امتیاز منفی، deadline دقیق حراج، Insurance، اتمام pool تعویض، Undo وسط حراج، State خراب localStorage، ۱۰۰ چرخه serialize/hydrate، Tie-break نامعتبر، Coup با امتیازهای منفی، دوئل تکراری و تلاش برای ردکردن مستقیم نوبت.

## محدودیت باقی‌مانده
این تست‌ها Engine/State را به‌شکل خودکار پوشش می‌دهند. Production build واقعی و لمس/Resume واقعی Safari روی iPhone هنوز تست محیط واقعی نشده است.

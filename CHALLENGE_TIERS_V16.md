# Parsayan V16 — Challenge Tier & Scoring Audit

## تغییر اصلی
بانک ۶۰ چالش اکنون واقعاً با خانه‌های ۲۰۰/۳۰۰/۴۰۰/۵۰۰/۶۰۰ هماهنگ است. هر سطح دقیقاً ۱۲ چالش دارد و انتخاب خانه فقط از همان سطح چالش انتخاب می‌کند.

معیار سطح‌بندی: بار بداهه‌پردازی، پیچیدگی اجرا، تعداد محدودیت‌های همزمان، فشار اجتماعی/اجرایی و نیاز به ساخت روایت. «بامزه‌تر بودن» معیار سختی نیست.

- 200: 12 چالش ساده و سریع
- 300: 12 چالش با بداهه متوسط
- 400: 12 چالش چندمرحله‌ای/اجرایی‌تر
- 500: 12 چالش با ساخت روایت یا محدودیت بیشتر
- 600: 12 چالش سخت‌تر با نقش‌آفرینی/ترکیب چند قید

## منطق امتیاز
چالش موفق همان امتیاز خانه را می‌دهد و چالش ناموفق طبق سیستم اصلی بازی نصف ارزش خانه را کم می‌کند. بنابراین ارزش خانه اکنون با سختی واقعی بانک چالش هم‌راستا است.

## Guard جدید
حتی اگر UI یا state دستکاری شود، `selectQuestion` برای دسته چالش فقط آیتم‌هایی را می‌پذیرد که `challenge.value === selectedValue` است. پس انتخاب ۲۰۰ نمی‌تواند چالش ۶۰۰ تحویل دهد. در صورت مصرف تمام ۱۲ چالش یک سطح، recycle فقط در همان سطح انجام می‌شود.

## تست جدید
`tests/challengeTiersV16.test.mjs`: 24/24 PASS

پوشش: تعداد کل، ID یکتا، value معتبر، دقیقاً ۱۲ آیتم در هر پنج سطح، انتخاب واقعی از هر سطح، امتیازدهی هر سطح و recycle محدود به همان tier.

## Regression
همه تست‌های V15/V14/V13/V12 و تست‌های جریان بازی و Secret/Resume/Tie بعد از این تغییر دوباره PASS شدند.

## Build واقعی
`npm install --no-audit --no-fund` دوباره اجرا شد اما محیط پس از 240 ثانیه timeout داد و `node_modules` کامل ایجاد نشد. بنابراین production Vite build و Safari واقعی هنوز به‌عنوان PASS ثبت نشده‌اند.

import React from"react";export default function Question({s,reveal,judge,back}){let q=s.question;return <main className="screen"><button className="ghost" onClick={back}>→ برگشت</button><div className="questionMeta"><span>{s.cat}</span><b>{s.value}</b></div><h1 className="question">{q?.text}</h1>{q?.opts&&!s.optionsRevealed&&<button className="primary" onClick={reveal}>نمایش گزینه‌ها</button>}{s.optionsRevealed&&q?.opts&&<div className="options">{q.opts.map((o,i)=><div key={i}>{i+1}. {o}</div>)}</div>}<div className="judge"><button onClick={()=>judge(false)}>✕ غلط</button><button onClick={()=>judge(true)}>✓ درست</button></div></main>}